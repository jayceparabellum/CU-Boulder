
University of Colorado Boulder
Graded Assignment

Tree-Based Methods & Ensembles
Assessment monitored for academic integrity and AI-usage. Privacy Notice

What to expect
Due Jul 9, 12:59 AM MDT
1 of 2 attempts
every 4 hours

45 min per attempt
You passed!

To pass you need a grade of at least 80%.

98.57%
Submitted Jun 30, 5:00 PM MDT.

Tree-Based Methods & Ensembles
Graded Assignment • 45 min

DueJul 9, 12:59 AM MDT
Your grade: 98.57%
Your latest: 98.57%•
Your highest: 98.57%
To pass you need at least 80%. We keep your highest score.


1.
Question 1
Select all advantages of decision trees when used for regression tasks.


Handles missing values effectively.

Nice work
Correct! Decision trees can handle missing values effectively, which is a significant advantage.

Requires less data preprocessing.

Nice work
Correct! Decision trees usually require less data preprocessing, which is one of their advantages.

Provides high bias error.

Sensitive to small changes in data.

Easy to interpret and visualize.

Nice work
Correct! Decision trees are easy to interpret and visualize, making them a popular choice for regression tasks.

1 / 1 point

2.
Question 2
What is one advantage of using decision trees for classification tasks?

Decision trees require less data preprocessing compared to other methods.

Decision trees can only be used for binary classification.

Decision trees are immune to overfitting.

Decision trees always provide the most accurate predictions.

Nice work
Correct! Decision trees are versatile and can handle raw data without extensive preprocessing.

1 / 1 point

3.
Question 3
Which of the following is a common post-pruning technique used to enhance the generalization of decision trees?

Do you understand?.

Gradient descent pruning

Split-point pruning

Entropy reduction pruning

Cost complexity pruning

Nice work
Correct! Cost complexity pruning is a frequently used post-pruning technique to improve the performance of decision trees by removing branches that add little predictive power.

1 / 1 point

4.
Question 4
What are some disadvantages of using decision trees? Select all that apply.

Decision trees can be prone to overfitting.

Nice work
Correct! Without proper pruning, decision trees may overfit the training data.

Decision trees might not capture linear relationships well.

Nice work
Correct! Trees may struggle with linear relationships unless they are very deep.

Decision trees are difficult to interpret.

Decision trees require substantial computational resources.

1 / 1 point


5.
Question 5
Which strategies can help enhance decision tree generalization? Select all that apply.

Increasing the number of features in the dataset.

Implementing cost-complexity pruning.

Nice work
Correct! Pruning helps reduce overfitting and improves generalization.

Applying cross-validation techniques.

Building deeper trees to capture more patterns.

Choosing optimal hyperparameters for tree construction.

Nice work
Correct! Optimal hyperparameters improve tree performance.

You didn't select all the correct answers
0.8 / 1 point


6.
Question 6
How does bagging affect model variance in machine learning?

It decreases variance by averaging results from multiple models.

It maintains constant variance by stabilizing predictions.

It eliminates variance by using identical datasets for training.

It increases variance by using diverse model types.

Nice work
Correct! Averaging results is a key factor in reducing variance.

1 / 1 point


7.
Question 7
What is the role of bootstrap sampling in generating out-of-bag (OOB) validation sets?

It uses only a subset of features for validation.

It duplicates the entire dataset to increase sample size.

It creates independent datasets for training and validation.

It ensures that sampled data is balanced across all classes.

Nice work
Correct! Bootstrap sampling allows separate evaluation of model performance.

1 / 1 point


8.
Question 8
What is the main advantage of using Random Forests over a single decision tree in terms of reducing overfitting?

Random Forests utilize multiple decision trees and average their predictions, which reduces overfitting compared to a single tree.

Random Forests eliminate overfitting by ignoring certain data points during training.

Random Forests use more complex algorithms, which inherently reduce overfitting without the need for averaging.

Random Forests reduce overfitting by using only a single tree with optimized parameters.

Nice work
Correct! Random Forests average the predictions from multiple trees to mitigate overfitting.

1 / 1 point


9.
Question 9
Which of the following best illustrates the concept of 'wisdom of the crowds' in decision tree predictions?

Selecting predictions from the most frequently correct tree.

Aggregating predictions from multiple trees reduces errors.

Maximizing correlation between trees to ensure consistency.

Using the largest tree possible to improve prediction accuracy.

Nice work
Correct! Reflect on why combining multiple predictions enhances accuracy.

1 / 1 point


10.
Question 10
Which factors can be analyzed using Individual Conditional Expectation (ICE) plots? Select all that apply.

Local variations in predictions relative to changes in a particular feature.

Nice work
Correct! ICE plots are used to understand local changes in predictions based on feature adjustments.

Global interaction effects among all features in a dataset.

Impact of a single feature on individual predictions.

Nice work
Correct! ICE plots are designed to analyze how changes in one feature affect specific instances.

Variability in model predictions due to stochastic processes.

Sequential fitting process impacts in gradient boosting.

1 / 1 point


11.
Question 11
How does bagging differ from boosting in model training?

Bagging reduces variance by averaging predictions from multiple models, while boosting sequentially improves predictions by focusing on errors.

Bagging increases model complexity by combining weak learners, whereas boosting simplifies models by pruning trees.

Bagging is used primarily for regression tasks, whereas boosting is exclusive to classification.

Bagging uses a single model to minimize bias, while boosting uses multiple models to increase bias.

Nice work
Correct! You’ve correctly identified how bagging and boosting differ in their approach to model training.

1 / 1 point


12.
Question 12
Select all techniques that are commonly used in stochastic boosting to improve model performance.

Using a fixed learning rate throughout training.

Introducing randomness in the subset of training data.

Nice work
Correct! This technique aids in reducing variance in the model.

Increasing the number of trees to maximize predictive accuracy.

Randomly selecting a subset of features at each iteration.

Nice work
Correct! This method helps in diversifying the model and reducing overfitting.

1 / 1 point


13.
Question 13
Which factors should be considered when implementing early stopping in gradient boosting? Select all that apply.

Increasing the depth of trees with each iteration

Reducing learning rate significantly

Monitoring validation error

Nice work
Correct! Monitoring validation error is crucial to determine the optimal point to halt training and prevent overfitting.

Predefined number of iterations

Nice work
Correct! Setting a predefined number of iterations can help manage training time and avoid excessive computation.

1 / 1 point


14.
Question 14
Which parameter selection strategy is most effective for optimizing gradient boosted tree models?

Tuning learning rate and number of trees through cross-validation.

Selecting maximum depth based on training accuracy alone.

Adjusting minimum samples per leaf to improve model complexity.

Using default parameter settings as a baseline for all models.

Nice work
Correct! Cross-validation is key to finding the optimal learning rate and tree number for boosted models.

1 / 1 point




